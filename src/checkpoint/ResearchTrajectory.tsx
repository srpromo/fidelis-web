import type {CSSProperties} from 'react';
import type {TrajectoryPoint} from '../types';
export function ResearchTrajectory({ticker,points,animate=false}:{ticker:string;points:TrajectoryPoint[];animate?:boolean}){const t2=points[2],c={ticker,t3:points[3]};return <svg className="trajectory" viewBox="0 0 180 110" role="img" aria-label={`${c.ticker}: ${points.map(p=>`T${p.t} ${p.ops}/${p.rap}`).join(', ')} OPS/RAP`}><path className="trajectory-axes" d="M15 5V88H170"/><text x="1" y="10">RAP</text><text x="147" y="104">OPS</text><path className="prior-path" d={points.slice(0,3).map((p,i)=>`${i?'L':'M'}${15+p.ops*1.45} ${88-p.rap*.78}`).join('')}/><path className="movement-line" d={`M${15+t2.ops*1.45} ${88-t2.rap*.78}L${15+c.t3.ops*1.45} ${88-c.t3.rap*.78}`}/>{points.map(p=><circle key={p.t} className={p.t===3?'point-t1':'point-t0'} cx={15+p.ops*1.45} cy={88-p.rap*.78} r={p.t===3?4:3} style={animate&&p.t===3?{'--dx':`${(t2.ops-p.ops)*1.45}px`,'--dy':`${(p.rap-t2.rap)*.78}px`} as CSSProperties:undefined}/>)}</svg>}
